with "xmlada";

project Test is

   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnatwa");
   end Compiler;

--     package Linker is
--       for Default_Switches ("Ada") use ("-lxmlada");
--    end Linker;

end Test;
